# fill from: cd terraform && terraform output
all:
  children:
    workers:
      hosts:
        fraudguard-worker-0:
          ansible_host: 10.40.10.10
          ansible_user: ec2-user
        fraudguard-worker-1:
          ansible_host: 10.40.10.11
          ansible_user: ec2-user
  vars:
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o ProxyCommand="ssh -W %h:%p -q ec2-user@BASTION_PUBLIC_IP"'
    redis_endpoint: REPLACE_ME
